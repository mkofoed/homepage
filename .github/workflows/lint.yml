name: Lint and Format

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@0aac1192d00d285ee0f4f4f7aafa099c17a7c4e0 # v5
        with:
          python-version: "3.14"
          allow-prereleases: true

      - name: Install dependencies
        run: |
          pip install uv
          uv pip install --system -e . --no-cache

      - name: Run Ruff (fix)
        run: |
          ruff check --fix .
          ruff format .

      - name: Commit fixes
        uses: stefanzweifel/git-auto-commit-action@8621da0b4fc31d342dbdbedde51917cca21e8c9c # v5
        with:
          commit_message: "style: auto-fix linting issues [skip ci]"
          commit_options: "--no-verify"

      - name: Run Ruff (check strict)
        run: ruff check .

      - name: Run Mypy
        env:
          SECRET_KEY: "ci-dummy-key-not-used-in-production"
        run: mypy . --config-file pyproject.toml
